// Datatypes
    // Primitive Datatypes
        // String
        let name = "John";

        // Number
        let age = 25;

        // Boolean (true or false)
        let isMarried = true;

        // Null (empty)
        let car = null;
 
        // Undefined (not declared, but can be updated)    
        let job;
    

    // Non-Primitive Datatypes
        // Object
        let person = {
            name: "John",
            age: 25,
            isMarried: true
        };

        // Array
        let fruits = ["apple", "banana", "orange"];

        // Function (in this case, add function is a variable which contains a function)
        function add(a, b) {
            return a + b;
        }


    // open terminal and type "node Lesson1b.js" to run the code